spring:
  ai:
    model:
      chat: ollama
    ollama:
      # 本地运行时使用（Testcontainers 会覆盖）
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.1
          temperature: 0.7

logging:
  level:
    org.springframework.ai: DEBUG
